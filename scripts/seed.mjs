const seedItems = [
  "homepage content",
  "donation medicine catalog",
  "demo payment flow",
];

console.log(`Seed ready: ${seedItems.join(", ")}.`);
console.log("No database is required; demo data is bundled with the app.");
